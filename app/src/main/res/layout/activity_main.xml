<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/backgroundImageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/background_image" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:paddingTop="110pt"
        android:orientation="vertical">

        <Button
            android:id="@+id/photoTagger"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="10pt"
            android:onClick="launchPhotoTagger"
            android:text="Photo Tagger"
            android:backgroundTint="@android:color/holo_blue_dark"
            android:textSize="13pt" />

        <Button
            android:id="@+id/sketchTagger"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="10pt"
            android:onClick="launchSketchTagger"
            android:text="Sketch Tagger"
            android:backgroundTint="@android:color/holo_red_light"
            android:textSize="13pt" />

        <Button
            android:id="@+id/storyTeller"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:onClick="launchStoryTeller"
            android:text="Story Teller"
            android:backgroundTint="@android:color/holo_orange_light"
            android:textSize="13pt" />

    </LinearLayout>

</RelativeLayout>